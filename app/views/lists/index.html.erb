<h1>Blocitoff items</h1>

<h2>TO DO:</h2>

<% @todos.each do |t| %>
  <p>
    <strong><<%= t.description %></strong>
    <small><%= link_to "Mark as done", edit_item_path(t), :method => :put %></small>
  </p>
<% end %>


<h2>DONE TODOS:</h2>

<% @todone.each do |t| %>
  <p>
    <strong><<%= t.description %></strong>
    <small><%= link_to "Remove", t, :confirm => "It is ok to remove item from list?", :method => :delete %></small>
  </p>
<% end %>



<p><%= link_to "Add new TODO", new_item_path %></p>
